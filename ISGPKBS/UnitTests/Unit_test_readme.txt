To create a unit test on the code in the Game project you need to:
Add the files to UnitTests project by the "Add existing file" option.
Then for the #include tags use "../Game/<header>.h"

To run the tests use:
"TEST" (from the top menu) -> Run -> All Tests (CTRL + R, A)